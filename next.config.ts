import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false, // disables all dev indicators including the bottom-left icon
};

export default nextConfig;
